<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  controllerName="agenda.controller.Groups">
  <Page title="Maintain Groups" showNavButton="true" navButtonPress="onNavBack">
    <content>
      <SplitApp id="groupsSplitApp" mode="ShowHideMode">
        <masterPages>
          <Page title="Groups">
            <subHeader>
              <OverflowToolbar>
                <SearchField id="groupSearchField" width="100%" liveChange="onFilterGroups" placeholder="Filter groups"/>
                <ToolbarSpacer/>
                <Button id="addGroupButton" text="Add" type="Emphasized" press="onAddGroup"/>
              </OverflowToolbar>
            </subHeader>
            <content>
              <List
                id="groupsList"
                mode="SingleSelectMaster"
                selectionChange="onGroupSelect"
                items="{/groups}">
                <StandardListItem title="{name}" description="{description}" type="Active"/>
              </List>
            </content>
          </Page>
        </masterPages>
        <detailPages>
          <Page title="Group Details">
            <content>
              <VBox class="sapUiResponsiveContentPadding" renderType="Bare">
                <Label text="ID"/>
                <Input id="groupIdInput" value="{/selectedGroup/ID}" editable="false"/>
                <Label text="Name" class="sapUiSmallMarginTop"/>
                <Input id="groupNameInput" value="{/selectedGroup/name}" enabled="{= ${/selectedGroup} !== null }"/>
                <Label text="Description" class="sapUiSmallMarginTop"/>
                <TextArea id="groupDescriptionInput" value="{/selectedGroup/description}" rows="4" enabled="{= ${/selectedGroup} !== null }"/>
              </VBox>
            </content>
            <footer>
              <OverflowToolbar>
                <ToolbarSpacer/>
                <Button id="deleteGroupButton" text="Delete" type="Negative" press="onDeleteGroup" enabled="{= ${/selectedGroup} !== null }"/>
                <Button id="saveGroupButton" text="Save Changes" type="Emphasized" press="onSaveGroup" enabled="{= ${/selectedGroup} !== null }"/>
              </OverflowToolbar>
            </footer>
          </Page>
        </detailPages>
      </SplitApp>
    </content>
  </Page>
</mvc:View>
